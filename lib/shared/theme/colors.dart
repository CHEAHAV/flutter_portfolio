import 'dart:ui';

class AppColors {
  // ── Backgrounds ─────────────────────────────────────────────
  static const Color bgColor       = Color(0xFF0D1117);
  static const Color surfaceColor  = Color(0xFF161B22);
  static const Color cardColor     = Color(0xFF1C2230);

  // ── Brand / Accent ──────────────────────────────────────────
  static const Color primary       = Color(0xFF00F0FF);
  static const Color secondary     = Color(0xFF3D5AFE);
  static const Color tertiary      = Color(0xFF0F172A);
  static const Color accentCyan    = Color(0xFF00D4B8);
  static const Color accentPurple  = Color(0xFF7C3AED);

  // ── Text ────────────────────────────────────────────────────
  static const Color textPrimary   = Color(0xFFE6EDF3);
  static const Color textSecondary = Color(0xFF8B949E);
  static const Color neutral       = Color(0xFF94A3B8);

  // ── Border ──────────────────────────────────────────────────
  static const Color borderColor   = Color(0xFF30363D);

  // ── Status ──────────────────────────────────────────────────
  static const Color success       = Color(0xFF22C55E);
  static const Color warning       = Color(0xFFF59E0B);
  static const Color error         = Color(0xFFEF4444);
}

class AppColorsLight {
  // ── Backgrounds ─────────────────────────────────────────────
  static const Color bgColor       = Color(0xFFF6F8FA);
  static const Color surfaceColor  = Color(0xFFFFFFFF);
  static const Color cardColor     = Color(0xFFEFF2F5);

  // ── Brand / Accent ──────────────────────────────────────────
  // primary cyan darkened for contrast on white
  static const Color primary       = Color(0xFF0088A3);
  static const Color secondary     = Color(0xFF3D5AFE);
  // tertiary flipped to a light tint
  static const Color tertiary      = Color(0xFFE8F0FE);
  static const Color accentCyan    = Color(0xFF00897B);
  static const Color accentPurple  = Color(0xFF6D28D9);

  // ── Text ────────────────────────────────────────────────────
  static const Color textPrimary   = Color(0xFF1A2330);
  static const Color textSecondary = Color(0xFF57606A);
  static const Color neutral       = Color(0xFF4A5568);

  // ── Border ──────────────────────────────────────────────────
  static const Color borderColor   = Color(0xFFD0D7DE);

  // ── Status ──────────────────────────────────────────────────
  static const Color success       = Color(0xFF16A34A);
  static const Color warning       = Color(0xFFD97706);
  static const Color error         = Color(0xFFDC2626);
}